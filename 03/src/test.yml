---
- name: Test Terraform inventory
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Create test file
      ansible.builtin.file:
        path: /tmp/terraform-ansible-test
        state: touch
        mode: "0644"
